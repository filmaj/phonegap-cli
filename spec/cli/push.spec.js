/*
 * Module dependencies.
 */

var phonegap = require('../../lib/main'),
    CLI = require('../../lib/cli'),
    argv,
    cli;

/*
 * Specification: $ phonegap push
 */

describe('phonegap push', function() {
    it('should delegate to the phonegap/push module');
    it('should pass errors from the phonegap/push module to the provided callback');
});
